services:
  rtmeclsp:
    build: .
    ports:
      - "8080:8080"
    volumes:
      # Mount config so you can edit symbols/exchanges without rebuilding
      - ./config.yaml:/app/config.yaml:ro
    restart: unless-stopped
    environment:
      - RUST_LOG=info
